import * as SQLite from 'expo-sqlite';


var db = SQLite.openDatabase("StatesNCity.db") ;
var sts ;
var city ;
export default function AccessData(props){
    //for sts in Object.keys(props.data){

    //}
}